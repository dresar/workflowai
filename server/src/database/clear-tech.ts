import * as dotenv from 'dotenv'; dotenv.config(); import { neon } from '@neondatabase/serverless'; import { drizzle } from 'drizzle-orm/neon-http'; import * as schema from './schema'; const databaseConnection = neon(process.env.DATABASE_URL!); const db = drizzle(databaseConnection, { schema }); async function clear() { await db.delete(schema.technologies); console.log('Cleared technologies'); process.exit(0); } clear();
