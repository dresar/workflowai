import * as dotenv from 'dotenv'; dotenv.config(); import { neon } from '@neondatabase/serverless'; import { drizzle } from 'drizzle-orm/neon-http'; const db = drizzle(neon(process.env.DATABASE_URL!)); async function run() { try { await db.execute('ALTER TABLE technologies ADD CONSTRAINT technologies_name_unique UNIQUE(name);'); console.log('success'); } catch(e) { console.log(e); } process.exit(0); } run();
